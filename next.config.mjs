/** @type {import('next').NextConfig} */
const nextConfig = {
    eslint: {
      ignoreDuringBuilds: true,
    },
    images: {
      domains: ['wizam.awmtab.in'], 
    },
  };
  
  export default nextConfig;
  